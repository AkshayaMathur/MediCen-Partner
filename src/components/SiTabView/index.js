import React, {PureComponent} from 'react';

import {TabView} from 'react-native-tab-view';

class SiTabView extends PureComponent {
  render() {
    return <TabView {...this.props} />;
  }
}

export default SiTabView;
